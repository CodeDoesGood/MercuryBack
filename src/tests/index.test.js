/**
 * Commenting out the database tests for when deploying onto travis until a method of
 * connecting to the database for travis is created that is safe while also protecting
 * the database contents.
 */
// require('./database.test');
